{
  "name": "matcha",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix client",
    "dev": "concurrently \"npm run client\" \"npm run server\" ",
    "update": "git add * && git commit -am 'update' && git push",
    "pull": "git fetch origin main && git merge origin/main",
    "push": "git push origin backend:main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "latest"
  },
  "dependencies": {
    "nodemon": "^3.1.7"
  }
}
