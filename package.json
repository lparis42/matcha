{
  "name": "matcha",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run client\" \"npm run server\" ",
    "server:update": "git add . && git commit -m 'update' && git push --prefix server",
    "server:pull": "git fetch origin master && git merge origin/master --prefix server",
    "server:push": "git push origin backend:main --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "latest"
  },
  "dependencies": {
    "vite": "^5.2.13"
  }
}
